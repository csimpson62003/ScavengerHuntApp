import { View } from 'react-native';

export default function LogoutScreen() {
  // This screen will never be shown as we prevent navigation to it
  // and trigger the logout action instead
  return <View />;
}